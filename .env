# Leave unset to use same-origin `/api` with Vite proxy (avoids HTTPS mixed-content in dev).
# VITE_API_URL=http://localhost:3001

# Dev HTTPS options:
# - Default is HTTP (recommended for localhost): http://localhost:5173
# - For quick HTTPS (self-signed, browser warning): run `npm run dev:https` (sets VITE_BASIC_SSL=true)
# - For trusted HTTPS (no warning): set both paths below (e.g. mkcert) and run `npm run dev`
# VITE_SSL_CERT_PATH=
# VITE_SSL_KEY_PATH=

# Supabase Configurations
VITE_SUPABASE_URL=https://cpdmpfvhtwebzlbjbgvt.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_vVhTlCeTKDCMR_g400HGRw_WpL0CqD9
